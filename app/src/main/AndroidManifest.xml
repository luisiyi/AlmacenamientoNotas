<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gutierrezmorquecho.luis.mis_notas">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mis_notas">
        <activity android:name=".AgregarNotaActivity"></activity>
        <activity android:name=".Agregar_Nota" />
        <!--
        <activity android:name=".Agregar_Nota"
            android:label="@string/title_activity_agregar_nota"
            android:theme="@style/Theme.Mis_notas.NoActionBar" />
        -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>